#define _CRT_SECURE_NO_WARNINGS 1
#include<stdio.h>
int main()
{
//	变量的命名：
//		1.有意义
//		int age；
//		float salary；
//		2.名字必须是字母、数字、下划线组成，不能有特殊字符，同时不能以数字开头。
//		int 2b;//err
//		int _2b;//ok
//		3.变量名不能是关键字
//		常见关键字
		//auto   break   case   char   const   continue   default	  do   double   else   enum 
		//extern   float   for   goto   if   int   long   register   return   short   signed
		//sizeof   static   struct	switch   typedef   union	unsigned   void   volatile   while

		//		         
		//						 	 
		//			for 		if else		char								const 常属性			extern 申明外部符号的
		//			while		switch		short														register - 寄存器 --- 科普：鹏哥视频P20
		//			do while	case		int															static 静态的						 00:55:33开始
		//			break		default		long														return 函数返回值
		//			continue	goto		float
		//									double
		//									signed  有符号的
		//									unsigned 无符号的
		//									enum - 枚举
		//									struct - 结构体
		//									union - 联合体（共用体）
		//									void 无（函数的返回类型，函数参数）
		//									sizeof - 计算大小
		//									
	//	

	return 0;
}
